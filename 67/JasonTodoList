Define the shape: Create the list of coordinates (vertices) in C++.
Send it to the GPU: Set up a VAO and VBO to upload those coordinates to the graphics card.
Tell OpenGL to draw it: Add a draw call (like glDrawArrays) inside the render loop so the GPU actually renders the uploaded shape every frame.